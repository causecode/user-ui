import { IUserAction } from '../interfaces';
import { IFromJS } from 'react-hero';
export declare const initialState: IFromJS;
export declare const userReducer: (state: IFromJS, action: IUserAction) => IFromJS;
