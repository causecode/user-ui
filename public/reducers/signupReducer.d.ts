import { ISignupAction } from '../interfaces';
import { IFromJS } from 'react-hero';
export declare const initialState: IFromJS;
export declare const signupReducer: (state: IFromJS, action: ISignupAction) => IFromJS;
