import {CSS} from '../interfaces';

export const removeMarginAndPadding: CSS = {
    margin: '0px',
    padding: '0px',
};

export const pullRight: CSS = {
    float: 'right',
};

export const pullLeft: CSS = {
    float: 'left',
};

export const defaultPanelContainer: CSS = {
    maxWidth: '500px',
    margin: '0px auto',
    padding: '30px',
};

export const listStyle: CSS = {
    display: 'inline',
};

export const listContainer: CSS = {
    display: 'inline-block',
    width: '100%',
};

export const listItem: CSS = {
    width: '33%',
    display: 'inline-block',
};

export const defaultInputStyle: CSS = {
    padding : '15px',
    marginBottom: '15px',
};

export const defaultFooterContainer: CSS = {
    width: '100%',
    display: 'inline-block',
};

export const defaultTableStyle: CSS = {
    maxWidth: '700px',
    marginLeft: 'auto',
    marginRight: 'auto',
};
